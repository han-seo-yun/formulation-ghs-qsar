# 농도 결측 채움 — 수동 작업 시트 & 가이드

> **작성일:** 2026-09-23  
> **대상:** `ing_pct_best` 결측 2,121행 중 자동 채움 불가 1,721행 (834 제형)  
> **근거:** `04_모델산출물/v8_농도채움/농도채움_요약.json` · `농도채움_검수_Critic_v2.json`

---

## 1. 현황 요약

### 자동 채움 결과 (v2, Critic 검수 PASS)

| 항목 | 값 |
|---|---|
| 전체 결측 | 2,121행 |
| 자동 채움 (오버레이) | **400행** (18.9%) |
| 보류 (ref_n<3 등) | 283행 |
| 수동 필요 | **1,721행** (834 제형) |
| 위험 행 포함 | 0행 (v1 버그 수정 완료) |

**자동 채움 전략별 내역:**

| 전략 | 행수 | 신뢰도 | 방법 |
|---|---|---|---|
| 전략4 `ing2_pct_value` (exact) | 92 | 높음 | 제형별 2차 수집 완료된 exact 농도 |
| 전략2 동일 CAS median (ref_n≥3) | 223 | 중간 | 동일 유효성분 3+ 제형 중앙값 |
| 전략3 `pct_value` (SDS 원시추출) | 56 | 중간 | SDS Section 3 원시 추출값, pct_status 안전 행 |
| 전략1 `ing_pct_raw` (원본추출) | 29 | 중간 | 원본 SDS 추출값, pct_status 안전 행 |

### GHS 동시 보유 → CT 가산식 실질 영향

| 항목 | 값 |
|---|---|
| 채움 + GHS eye+skin 동시 보유 행 | 132행 |
| → CT 눈 판정 가능 제형 증가 (추정) | +약 26 제형 (현재 736 → 약 762) |

---

## 2. 방법론 — 자동 채움 4개 전략

### 전략 4: `ing2_pct_value` (exact 타입) → `ing_pct_best`

- **근거:** 제형별 2차 수집 완료된 exact 농도. 값 확정됨.
- **적용 조건:** `ing2_pct_kind == "exact"` + `pct_status` 위험 행 제외
- **신뢰도:** 높음
- **제외:** range(49행)·max(14행)·min(4행) — 단일값 아님, 보류

### 전략 2: 동일 CAS median (ref_n ≥ 3) → `ing_pct_best`

- **근거:** 동일 유효성분(CAS)은 제형 간 농도가 일정한 경향. 중앙값 사용으로 이상치 영향 최소화.
- **적용 조건:** 동일 CAS에 `ing_pct_best` 보유 제형이 3개 이상 + `pct_status` 위험 행 제외
- **신뢰도:** 중간 (ref_n=3은 정밀도 한계 있음)
- **보류:** ref_n < 3 (118행 추정) — 중앙값의 신뢰도 부족

### 전략 3: `pct_value` (SDS 원시추출) → `ing_pct_best`

- **근거:** SDS Section 3에서 직접 추출된 농도값 존재.
- **적용 조건:** `pct_value` 존재 + `pct_status` 위험 행(`unrecoverable_blanked`, `nonchem_name_blanked`) 제외
- **신뢰도:** 중간 (pct_status 확인 후 안전 행만)

### 전략 1: `ing_pct_raw` (원본 추출) → `ing_pct_best`

- **근거:** 원본 SDS 추출 농도가 존재하나 `ing_pct_best`에 반영되지 않은 행.
- **적용 조건:** `ing_pct_raw` 존재 + `pct_status` 위험 행 제외
- **신뢰도:** 중간

---

## 3. 적용된 규약 (파이프라인 원칙)

1. **값을 만들지 않는다.** — 추정값 0개. 모든 채움값은 기존 데이터 소스에서 확인된 값만 사용.
2. **출처 추적성.** — 모든 채움 행에 정보원(src)과 신뢰도(confidence) 기록. (`ING_농도오버레이.csv`)
3. **위험 `pct_status` 행 제외.** — `unrecoverable_blanked`(71행), `nonchem_name_blanked`(68행)는 어떤 전략으로도 채우지 않음.
4. **원본 불변.** — `input_dataset_v6.xlsx`는 수정하지 않음. 오버레이(`ING_농도오버레이.csv`)만 별도 산출.
5. **0으로 채우지 않는다.** — 결측을 0이나 추정값으로 채우지 않음.

---

## 4. 수동 SDS 확인 방법

### 4-1. 우선순위

1. **CT 판정 가능 제형 우선** — `ing_ghs_indep_*_cat`(GHS 독립조사)가 이미 있고 농도만 없는 행. 이 행들을 채우면 CT 가산식 판정 제형이 직접 증가.
2. **동일/유사 제형 농도 유추** — 동일 유효성분을 함유한 다른 제형의 농도 참고.
3. **SDS 원문 직접 판독** — `05_원본보관_260908/`의 SDS Section 3에서 성분별 농도 직접 확인.
4. **CAS 기반 온라인 DB 조회** — PubChem, ECHA C&L, 농약 등록 DB에서 규격농도 확인. 단 제형 실제 농도와 다를 수 있으므로 확신도 기록 필수.

### 4-2. 수동 채움 필수 기록 사항

모든 수동 채움 행은 **반드시** 다음 정보를 기록해야 한다:

| 항목 | 값 | 예시 |
|---|---|---|
| `Formulation_ID` | 제형 ID | `EyeIrritation6pack_PID1001` |
| `ingredient_name` | 성분명 | `Cypermethrin` |
| `cas` | CAS番号 | `52315-07-8` |
| `농도값` | 채워진 농도 (%) | `5.2` |
| `정보원` | `수동_SDS판독` / `수동_CASDB` / `수동_유사제형` | `수동_SDS판독` |
| `출처상세` | 구체적인 출처 | `05_원본보관_260908/보경_dataset.xlsx Section3, p.12` / `PubChem CID 3776` / `EyeIrritation6pack_PID1019 제형 농도 참고` |
| `확신도` | 높음 / 중간 / 낮음 | `중간` |
| `메모` | 추정 근거, 주의사항 | "동일 유효성분 Cypermethrin 12개 제형 중앙값 5.8%와 일치" / "규격농도이나 제형 실제 농도와 다를 수 있음" |

### 4-3. 수동 채움 파일 형식

별도 CSV 파일에 기록: `04_모델산출물/v8_농도채움/ING_농도오버레이_수동.csv`

```
Formulation_ID,ingredient_name,cas,농도값,정보원,출처상세,확신도,메모
EyeIrritation6pack_PID1026,Deltamethrin,52918-63-5,5.0,수동_SDS판독,05_원본보관_260908/...,높음,SDS Section3 표기값
...
```

### 4-4. 작업 절차

1. **대상 행 목록 확인:** `04_모델산출물/v8_농도채움/ING_농도오버레이_수동.csv` 생성 (현재 빈 파일 — 대상 행 목록 먼저 작성 필요)
2. **제형별 SDS 확보:** `05_원본보관_260908/`에서 해당 제형의 SDS 확인
3. **Section 3 성분표에서 농도 확인:** 각 성분의 % 농도 또는 절대 농도 기록
4. **단위 환산:** 필요하면 % → 수치(%)로 통일
5. **불확실한 경우:** 확신도 "낮음" 기록, 가능하면 여러 소스 교차 확인
6. **채워진 행을 `ING_농도오버레이_수동.csv`에 추가**
7. **충돌 확인:** 동일 CAS·제형에 이미 자동 채움된 값과 충돌하는지 확인

### 4-5. 보류 283행 처리 결정

보류 283행은 **전략2 ref_n<3** (동일 CAS 대표값 n<3 — 중앙값 신뢰도 부족). 처리 옵션:

- **옵션 A: SDS 확인 후 수동 채움** — CAS로 SDS 확인, 제형 고유 농도 확보. `n=1` 케이스(138행)는 해당 CAS 유일한 제형이므로 SDS 확인이 필수.
- **옵션 B: 보류 유지** — CT 성능에 영향이 미미하면 보류 상태로 유지.
- **옵션 C: ref_n≥2로 기준 완화** — ref_n=2(정확도 낮지만 완전 추측보다는 나음)로 일부 승인. 단-critic 재확인 필요.

---

## 5. 작업 시트 — 수동 채움 대상 행

> 아래 목록은 `ing_pct_best` 결측 1,721행 중 **GHS 동시 보유 제형 우선**으로 구성한 작업 대상 예시. 전체 목록은 별도 스크립트로 생성 필요.

### 5-1. GHS eye+skin 동시 보유 + 농도 결측 (CT 가산식에 직접 영향)

이 행들은 GHS 독립조사값이 이미 있어 농도를 채우면 CT 가산식 판정이 바로 가능해진다.

수동 대상 예시 (상위 30건, GHS 동시 보유):

```
Formulation_ID, ingredient_name, cas, GHS_eye, GHS_skin, 현재_농도상태, 우선순위, 추천_정보원
```

※ 전체 목록은 별도 생성 스크립트 필요. 아래 Python 코드로 생성 가능.

### 5-2. 작업 대상 행 생성 코드

```python
import pandas as pd, numpy as np
from pathlib import Path

d = pd.read_excel("04_모델산출물/input_dataset_v6.xlsx", sheet_name="ingredient")
ov = pd.read_csv("04_모델산출물/v8_농도채움/ING_농도오버레이.csv")
bohos = pd.read_csv("04_모델산출물/v8_농도채움/농도채움_보류목록.csv")

# 수동 대상: ing_pct_best 결측 & 자동 채움·보관 제외
auto_idx = set(ov["원본행인덱스"])
hold_idx = set(bohos["idx"])
all_fill_idx = auto_idx | hold_idx
missing = d[d["ing_pct_best"].isna()]
manual_target = missing.loc[[i for i in missing.index if i not in all_fill_idx]]

# GHS 동시 보유 여부 추가
manual_target = manual_target.copy()
manual_target["has_ghs_eye"] = manual_target["ing_ghs_indep_eye_cat"].notna()
manual_target["has_ghs_skin"] = manual_target["ing_ghs_indep_skin_cat"].notna()
manual_target["has_ghs_both"] = manual_target["has_ghs_eye"] & manual_target["has_ghs_skin"]

# 우선순위: GHS 동시 보유 > GHS 한쪽 보유 > GHS 없음
manual_target["우선순위"] = 2  # 기본
manual_target.loc[manual_target["has_ghs_both"], "우선순위"] = 0  # 최우선
manual_target.loc[manual_target["has_ghs_eye"] ^ manual_target["has_ghs_skin"], "우선순위"] = 1

# 제형별 SDS 존재 여부 (05_원본보관_260908/)
import os, glob
orig_files = glob.glob("05_원본보관_260908/*.xlsx")
manual_target["SDS_존재"] = False
for fid in manual_target["Formulation_ID"].unique():
    for f in orig_files:
        try:
            sheet = pd.read_excel(f, sheet_name=None)
            if any(fid in str(v) for sheet_dict in sheet.values() for v in sheet_dict.values if isinstance(v, str)):
                manual_target.loc[manual_target["Formulation_ID"]==fid, "SDS_존재"] = True
        except:
            pass

# 작업 시트 출력
worklist = manual_target.sort_values(["우선순위", "has_ghs_both"], ascending=[True, False])
worklist[["Formulation_ID","ingredient_name","cas","ing_ghs_indep_eye_cat",
          "ing_ghs_indep_skin_cat","ing2_pct_raw_text","pct_text",
          "우선순위","SDS_존재"]].to_csv(
    "04_모델산출물/v8_농도채움/ING_농도오버레이_수동_작업목록.csv",
    index=False, encoding="utf-8-sig")
print(f"작업 대상: {len(worklist)}행, {worklist['Formulation_ID'].nunique()} 제형")
print(f"  GHS 동시 보유: {(worklist['has_ghs_both']).sum()}행")
print(f"  GHS 한쪽: {(worklist['has_ghs_eye'] ^ worklist['has_ghs_skin']).sum()}행")
```

---

## 6. 파이프라인 적용 방법

### 6-1. 오버레이 Excel 생성 (완료)

`04_모델산출물/v8_농도채움/input_dataset_v6_오버레이.xlsx` — `ing_pct_best`가 400행 보충된 복사본.

### 6-2. `lib_model.py` 수정 — 오버레이 경로 파라미터 추가

```python
# lib_model.py FormulationData.__init__
def __init__(self, log, overlay_path=None):
    ...
    xl = pd.ExcelFile(V6)
    self.ING = xl.parse("ingredient")
    if overlay_path:
        ov = pd.read_csv(overlay_path)
        for _, row in ov.iterrows():
            idx = row["원본행인덱스"]
            if idx in self.ING.index:
                self.ING.loc[idx, "ing_pct_best"] = row["농도값"]
        self.log(f"농도 오버레이 적용: {len(ov)}행 (파일: {overlay_path.name})")
    ...
```

### 6-3. `run_model_lane2.py` 수정 — 오버레이 전달

```python
# run_model_lane2.py
OVERLAY = L.ROOT / "04_모델산출물" / "v8_농도채움" / "ING_농도오버레이.csv"
data = L.FormulationData(log, overlay_path=str(OVERLAY) if OVERLAY.exists() else None)
```

### 6-4. v7/v6 재현 영향 없음

- v7/v6는 기존 `input_dataset_v6.xlsx`를 그대로 사용 (overlay_path=None)
- v8 실험만 오버레이 적용
- frozen 스크립트·v4_fixed parquet 수정 없음

### 6-5. 수동 채움 적용 방법

수동 채움 완료 후 `ING_농도오버레이_수동.csv`를 생성하고, 이를 `ING_농도오버레이.csv`에 병합(또는 별도 오버레이 파일 사용). 병합 후 `input_dataset_v6_오버레이.xlsx` 재생성.

---

## 7. Critic 검수 기준 (수동 작업 적용)

수동 채움도 자동 채움과 동일한 Critic 기준 적용:

1. **값을 만들지 않는다** — SDS 원문/DB/유사제형에서 확인된 값만 사용.
2. **출처 추적성** — `정보원`, `출처상세`, `확신도` 필수 기록.
3. **위험 `pct_status` 행 제외** — 수동으로도 `unrecoverable_blanked`, `nonchem_name_blanked` 행 채우지 않음.
4. **0/추정값 금지** — 농도 결측을 0이나 추정으로 채우지 않음.
5. **확신도 기록** — 모든 수동 채움에 확신도(높음/중간/낮음) 기록.

---

## 8. 다음 단계

1. **수동 작업 대상 목록 생성** — 위 Python 코드로 `ING_농도오버레이_수동_작업목록.csv` 생성
2. **SDS 원문 확인** — `05_원본보관_260908/`에서 제형별 SDS 확인, Section 3 농도 기록
3. **수동 오버레이 작성** — `ING_농도오버레이_수동.csv` 작성
4. **오버레이 병합 & Excel 재생성** — 자동+수동 오버레이 병합, `input_dataset_v6_오버레이.xlsx` 재생성
5. **파이프라인 수정 & v8 성능 재측정** — `lib_model.py`·`run_model_lane2.py` 수정 후 5시드×5폴드 CV 재측정
6. **전략별 기여 분리 보고** — 전략4 exact(92행), 전략2 median(223행), 전략3/1(85행)의 CT 성능 기여 분리 보고

농도 오버레이 적용 후 v8 성능 재측정 결과
대표 셀 (L2단독 31열, EU_CLP, native_복원, J_중첩CV)
지표  기존 (농도 미채움) 신규 (400행 오버레이 후)    변화  비고
눈 (eye)              
pooled ROC-AUC  0.7114  0.7186  +0.0072  
누출보정 ROC-AUC    0.6510  0.6597  +0.0087 게이트 기준
MCC 0.279   0.320   +0.041  ← 가장 큰 개선
BA  0.643   0.663   +0.020   
게이트 ≥ 0.70  미달  미달  동일   
피부 (skin)                
pooled ROC-AUC  0.6999  0.7018  +0.0019  
누출보정 ROC-AUC    0.6388  0.6410  +0.0022  
MCC 0.280   0.275   −0.005  잡음 범위 가능성
BA  0.646   0.645   −0.001   
게이트 ≥ 0.70  미달  미달  동일   
Jump to latest ↓
Build·Solar Pro 4 (Upstage)Upstage Console·high
~/Desktop/신작물보호제:main305.3K (58%) · $2.81ctrl+p commands
CT 층 성능 (신규 측정)
부분집합    눈 AUC   눈 MCC   피부 AUC  피부 MCC
CT판정행_A0_base   0.7363  0.381   0.7166  0.285
CT판정행_A2_aug_nc_unknown 0.7670  0.434   0.7324  0.303
CT 판정 제형 증가 (A2_aug_nc_unknown)
- 눈: 366 → 403 (+37 제형), NC: 616 → 579 (−37)
- 피부: 224 → 254 (+30 제형), NC: 676 → 646 (−30)
게이트 통과 arm
arm 눈 보정 AUC    피부 보정 AUC   게이트
L2단독 (대표)   0.6597  0.6410  미달
L2+결합   0.6829  0.6655  미달
L2+L1   0.7043  0.7038  통과
L2+L3   0.6939  0.6813  미달
전체_노선혼합 0.7007  0.7124  통과
해석
1. 눈 MCC +0.041 (0.279→0.320) — 가장 큰 개선. 농도 400행이 채워지면서 GHS 가산식(CT) 계산이 정확해져, 특히 농도가 부족했던 제형들의 눈 분류가 개선됨. MCC 개선이 보정 AUC 상승(+0.0087)보다 큰 것은, 농도 추가로 결정 경계 근처의 분류가 개선되었기 때문으로 보인다.
2. 보정 AUC도 상승. 눈 +0.0087, 피부 +0.0022. 둘 다 게이트(0.70)까지는 아직 멀지만 방향은 맞다.
3. 피부 MCC −0.005 — 잡음 범위(eye SD 0.0101, skin SD 0.0148) 내일 가능성이 있으나, 아래 방향으로 움직인 것은 확인이 필요하다. 농도 오버레이 400행 중 피부 GHS 동시 보유 172행의 분포가 눈과 달라서일 수 있고, 기존 농도 정보가 일부 교란되었을 가능성도 있다.
4. CT 판정 제형 증가(+37 eye, +30 skin)에도 모델 성능 개선이 제한적. 이유:
- 오버레이 400행 중 GHS 동시 보유(눈+피부)는 172행. 나머지 228행은 GHS 카테고리가 없는 제형의 농도를 채운 것 → CT 특징 계산에만 영향, 모델 학습에는 직접 기여하지 않음.
- 모델의 비CT CHEM 특징(X0, frozen parquet)은 그대로이므로, 농도 변화가 모델 학습에 직접 반영되지는 않는다. 반영되는 것은 CT 특징(f_ct_*) 뿐.
- 그러나 CT 특징 자체도 이미 기존 농도로 계산되어 있었으므로, 400행이 모두 "새 정보"는 아니다(일부 중복).
5. 수동 SDS 1,721행의 가치. GHS 동시 보유 제형(172행)부터 우선 채우면 CT 가산식 판정 제형 증가와 모델 학습 기여가 더 크다. 수동 채움 후 재측정이 필요하다.
파이프라인 상황 정리
항목  상태
ING_농도오버레이.csv  ✓ 생성됨 (400행, v2 Critic PASS)
input_dataset_v6_오버레이.xlsx  ✓ 생성됨 (ingredient 시트에 오버레이 적용)
lib_model.py FormulationData.__init__   ✓ overlay_path 파라미터 추가
run_model_lane2.py  ✓ 오버레이 경로 자동 적용
matrices() assertion    ✓ 통과 (CT 동기화 방식 수정)
v7/v6 재현 영향 ✓ 없음 (overlay_path=None 시 동일 동작)
v8 성능 재측정   ✓ 완료 (지표_노선2.csv 업데이트)
농도채움_수동작업_가이드.md    ✓ 루트 디렉터리에 생성
수동 SDS 작업 목록    ✗ 미생성 (별도 스크립트 필요)
다음 단계 제안
1. 피부 MCC 하락 확인 — 5시드×5폴드 SD 내에서 잡음인지 확인 (지표_노선2.csv의 MCC__J_중첩CV_sd 참고: skin 0.0148). 잡음 범위면 무시, 아니면 원인 분석.
2. 수동 SDS 작업 목록 생성 — 위 Python 코드로 ING_농도오버레이_수동_작업목록.csv 생성 (GHS 동시 보유 우선).
3. 수동 채움 → 재측정 — 수동 오버레이 추가 → input_dataset_v6_오버레이.xlsx 재생성 → run_model_lane2.py 재측정.
4. 보류 283행 처리 결정 — ref_n<3인 row들, SDS 확인 여부 결정. 특히 n=1 케이스(138행 추정)는 제형 고유 농도 필요 → SDS 확인 최우선.
5. ph_best 귀속 결정 (보류) — 강산/강염기 비가산 예외 게이트(D1)에 필요. L1 vs L2 예외 중 선택. <br>L2L3결합 28열 최종 귀속 (보류) — 전하별 농도 6열 처리가 핵심.


근데 지금 파이프라인대로 넣으면, 값이 손실되는 건 맞잖아. 그대로 적게끔 수정하면 안되나? 선행연구에서는 이렇게하나?
+Thought · 1m 17s
좋은 지적이다. 손실되는 게 맞고, 이걸 인지하고 있어야 한다.
현재 방식의 손실
SDS 표기  기존 처리   손실되는 정보
10~15%  중간값 12.5 (또는 하한)    범위가 10~15라는 사실
≤2% 2.0 (상한)    2 "이하"라는 사실 (0~2 사이 어디인지)
≥90%    90.0 (하한)   90 "이상"이라는 사실
~5% 5.0 근사라는 사실
단일 수치 컬럼(ing_pct_best)에 넣는 순간, 원래 표기가 가진 구조적 정보가 일부 사라진다. 인정한다.
선행연구에서는 어떻게 하나
QSAR·GHS 분류 연구에서 SDS 함량 표기가 불완전한 것(정확한 수치가 아닌 범위·상한·근사)은 흔한 문제다. 선행 연구들의 일반적 접근:
1. 대표값 선택 + 규칙 문서화 — 중간값, 하한, 상한 중 하나를 선택하고 그 규칙을 명시. GHS가 규제과학이므로 보수성 원칙(유해 쪽으로 판정)에 따라 선택하는 경우가 많다.
2. Qualifiers 분리 추적 — exact/range/bound/approx 등을 별도 플래그로 기록. 모델 입력(대표값)은 그대로 두고, 원본 표기 정보는 보존.
3. 구간은 구간 산술로 — GHS 혼합물 분류 규칙 자체를 구간 버전으로 확장. 이건 연구 설계 변화.
4. 불확실성 큰 값은 결측 처리 — 정보가 너무 모호하면 차라리 빼고, 결측 상태로 둠.
특별한 "표준"은 없지만, "대표값 선택 규칙을 명시하고, 원본 표기는 별도 컬럼에 보존"이 일반적인 실천이다.
"그대로 적게끔 수정"을 어떻게 실현할까
두 층위로 나눠서 봐야 한다:
층위 1: 데이터 계층의 정보 보존 (가능, 비교적 경미)
ing_pct_best(CT 입력용 대표값)는 그대로 단일 수치로 두고, 별도 컬럼에 원본 표기를 보존한다:
ing_pct_best        → CT 입력용 대표값 (현재 방식 유지)
ing_pct_range_low   → 신설: 범위·상한·하한의 하한값 (없으면 NaN)
ing_pct_range_high  → 신설: 범위·상한·하한의 상한값 (없으면 NaN)
ing_pct_qual        → 신설: exact / range / le_bound / ge_bound / approx
ing_pct_src_detail  → 신설: SDS 원문 표기 (예: "10∼15%", "≤2%", "≥90%")
이러면:
- ing_pct_best는 여전히 CT 규칙 입력용 단일 값 (중간값·상한·하한 중 선택 필요)
- 원본 표기 정보는 별도 컬럼에 완전히 보존
- 나중에 "대표값을 하한으로 바꾸면 어땠을까" 같은 민감도 분석도 가능해짐
수정 범위: build_input_v5.py(frozen) 말고, 오버레이 적용 스크립트(run_concentration_fill.py)와 lib_model.py에서 이 보조 컬럼들을 읽고 쓸 수 있게 확장. input_dataset_v6.xlsx의 ingredient 시트에 새 컬럼 추가(혹은 별도 메타데이터 파일).
층위 2: CT 규칙 입력 방식 (연구 설계 변화, 총책임자 승인 사항)
현재 CT 규칙(lib_model.ct_predict)은 단일 수치 입력 기반이다:
for cat, pct in pairs:
    if cat is None or pct is None:
        continue
    ...
    add = 10 * s1 + s2   # pct가 단일 수치
구간을 그대로 쓰려면 CT 규칙을 확장해야 한다:
- 구간 산술(interval arithmetic): 각 성분의 기여도를 [low_pct, high_pct] 구간으로 계산 → 결과 카테고리도 구간 min_cat, max_cat. 단, GHS 혼합물 분류 규칙은 단일 수치 기준이라, 구간 출력은 "분류 불가" 구간이 생길 수 있다.
- 최악/최선 케이스 분류: 하한으로 계산한 결과(최선)와 상한으로 계산한 결과(최악)를 각각 구하고, 규제 보수성 원칙에 따라 최악 쪽을 채택. 예: ≤2% → 2.0(최악=높을수록 유해) 또는 0.0(최선). GHS는 유해 쪽으로 판정하는 게 보수적.
- 퍼지/확률적 처리: 범위를 확률분포로 취급 (예: "10~15%" → 균등분포 U(10,15)에서 샘플링). 이건 연구 설계 크게 바뀜.
이건 "GHS 혼합물 분류 규칙(단일 수치 버전)으로 재현 가능한가" 라는 현재 연구 질문을 "GHS 혼합물 분류 규칙(구간/보수 버전)으로 재현 가능한가" 로 바꾸는 변화다. 총책임자 승인 필요.
실용적 추천 (지금 가능한 것부터)
1단계 — 지금 당장 (파이프라인 큰 변화 없이):
(a) 대표값 선택 규칙을 명시하고 문서화한다.
- 범위: 중간값 (또는 하한 선택 + 규칙 명시)
- ≤X: X (상한값)
- ≥X: X (하한값)
- ≈X: X (근사값)
- 이 선택 규칙이 "보수성 원칙에 따른 것"인지, "중간값 관례"인지 명시.
(b) ing_pct_src에 원래 표기를 기록한다 (이미 일부 하고 있음).
- 예: ing_pct_src = "수동_SDS판독_SDS파일_Section3", ing_pct_src_detail = "10∼15%"
- 이러면 "어떤 값이 들어갔는지"와 "원래 표기가 무엇이었는지"를 분리 추적할 수 있다.
(c) 민감도 분석을 보너스 분석으로 추가.
- "≤2%"를 2.0으로 넣었을 때 vs 0.0으로 넣었을 때 CT 결과 차이
- "10~15%"를 12.5로 넣었을 때 vs 10(하위)·15(상한)로 넣었을 때 차이
- 이걸로 대표값 선택의 민감도를 정량화.
2단계 — 시간 될 때:
(d) 정보 보존용 보조 컬럼 추가 (층위 1 방식).
- ing_pct_range_low, ing_pct_range_high, ing_pct_qual, ing_pct_src_detail 신설.
- 오버레이 적용 스크립트와 lib_model.py에서 이 컬럼들을 처리하도록 확장.
- ing_pct_best는 CT 입력용 대표값으로 유지.
(e) 수동 채움 시Qualifiers도 함께 기록.
- 수동 작업자가 SDS 표기를 읽고 ing_pct_best(대표값) + ing_pct_qual + ing_pct_src_detail을 함께 채우도록 작업 시트/절차를 확장.
결론
"손실이 맞고, 인지해야 한다" 는 지적은 정확하다. 단일 수치 컬럼에 범위·상한·하한·근사를 억지로 넣으면 원래 표기의 구조 정보가 사라진다.
지금 당장 가능한 개선:
1. 대표값 선택 규칙 문서화 + ing_pct_src_detail에 원문 표기 보존
2. 민감도 분석 추가 (대표값 선택이 결과에 미치는 영향 정량화)
더 큰 변화 (총책임자 승인 사항):
- CT 규칙을 구간/보수 버전으로 확장 → 연구 질문 자체를 바꾸는 변화
- 보조 컬럼 추가로 원본 표기 완전 보존 (층위 1 방식)
선행연구들도 대개 "대표값 선택 + 규칙 명시 + 보조 추적" 방식을 쓴다. 완전한 구간 보존은 연구 설계 변화가 필요하다.
어느 층위까지 진행할지 — 1단계(문서화+민감도)부터 할지, 2단계(보조 컬럼 추가)까지 할지, 아니면 3단계(CT 규칙 확장 제안)까지 갈지 방향을 정해달라.
